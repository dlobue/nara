
w = sconn.query_field('content', 'cobbler')

result_data = [a.data for a in sconn.search(w, 0, 10, collapse='thread', sortby='-date')]

threadidlist = map(lambda x: x['thread'][0], result_data)
newq2 = sconn.query_composite(sconn.OP_OR, map(lambda x: sconn.query_field('thread', x, sconn.OP_OR), threadidlist))
results = sconn.search(newq2, 0, 9000, checkatleast=-1, sortby='-date')

lzthread = lazythread.lazy_thread()
lzthread.thread([x.data for x in results])


conversation_view:
walker([
message_widget
message_widget
message_widget
])


message_widget (logical container w/caching for its own local objects)
message_widget.headers (search results)
message_widget.body (machined_widget - body state)
message_widget.quote (machined_widget - quote state)
message_widget.block_quote (folding/unfolding container of all block_quote machined widgets)
message_widget.attachment (etc)
message_widget.html (etc)

